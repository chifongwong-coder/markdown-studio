# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log

# Build artifacts
*.zip
build/
dist/

# Test artefacts: PDF exports produced by clicking the PDF button while
# iterating on print CSS. Not source.
*.pdf

# Store-submission screenshot masters (we only track the 1280x800 deliverables)
screenshots/*-original.png

# Local-only dev fixtures and drafts — not part of the shipped extension.
test.md
test-extract.mjs
cjk-test.md
comments-test.md
docx-test.md
font-test.md
mermaid-test.md
STORE_LISTING.md

# Note: vendor/ is downloaded by install.sh but deliberately committed:
#   1. Chrome Web Store submission requires every dependency to ship in the zip.
#   2. Pinning the files prevents CDN drift between re-runs of install.sh.
